import Layout from '../components/layout/layout';
import MessagesContent from './[id]/components/messages';

export default function MessengerContent() {
	return (
		<Layout>
			<MessagesContent />
		</Layout>
	);
}
